# JR MineralForge – Python Dependencies
# Team JR – Advancing Mineral Discovery with Robust AI & Open Australian Geodata
#
# Install: pip install -r requirements.txt
# GPU variant: set EMBEDDING_DEVICE=cuda and install torch with CUDA support

# ── Core LangChain ────────────────────────────────────────────────────
langchain>=0.3.0,<1.0
langchain-classic>=0.1.0
langchain-community
langchain-core>=0.2.0
langchain-ollama           # or whatever LLM integration you're using
langchain-openai>=0.1.0      # optional, for OpenAI backend
langchain-anthropic>=0.1.0   # optional, for Anthropic backend

# ── Vector Store & Embeddings ─────────────────────────────────────────
faiss-cpu>=1.7.4              # use faiss-gpu if CUDA available
sentence-transformers>=2.7.0
huggingface-hub>=0.23.0
transformers>=4.41.0

# ── Document Loading ──────────────────────────────────────────────────
pypdf>=4.2.0
unstructured>=0.14.0          # for complex PDF/report extraction

# ── Geospatial ────────────────────────────────────────────────────────
geopandas>=0.14.0
rasterio>=1.3.10
pyproj>=3.6.1
shapely>=2.0.4
fiona>=1.9.6
xarray>=2024.5.0
scipy>=1.13.0
pywavelets>=1.6.0             # wavelet denoising
folium>=0.17.0                # interactive maps

# ── ML / Data Science ─────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.5.0
xgboost>=2.0.3
lightgbm>=4.3.0
shap>=0.45.0
mlflow>=2.13.0

# ── Visualisation ─────────────────────────────────────────────────────
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0

# ── UI ────────────────────────────────────────────────────────────────
gradio>=4.36.0

# ── HTTP / Networking ─────────────────────────────────────────────────
requests>=2.32.0
aiohttp>=3.9.0
tqdm>=4.66.0

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv>=1.0.1
pyyaml>=6.0.1
rich>=13.7.0                  # colourful CLI output
click>=8.1.7

# ── Dev / Testing ─────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
black>=24.4.0
ruff>=0.4.0
